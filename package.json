{
  "name": "demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "install":"cd backend && npm install && cd .. && cd frontend && npm install",
    "start-backend": "cd backend && npm run dev-with-env",
    "start-frontend": "cd frontend && npm run dev"
  }
}
